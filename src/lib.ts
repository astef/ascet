export type AscetOptions = {
  elementId: string;
};

export function init(options: AscetOptions) {
  //     window.addEventListener("load", (ev) => {});
  //   window.addEventListener("resize", (ev) => {
  //   });
  //   window.addEventListener("mousedown")
}
